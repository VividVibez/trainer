# Pinned to the versions the app was developed and tested against.
# To capture the EXACT versions currently running on the Pi instead, run on the Pi:
#     ~/trainer/venv/bin/pip freeze > requirements.txt
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.51
gunicorn==23.0.0
